declare type PackageSyncOptions = {
    workingDir: string;
    cacheDir: string;
    packageFilePath: string;
    configFilePath: string;
};
declare type PackageSyncResult = {
    result: 'equal';
} | {
    result: 'diff';
} | {
    result: 'cacheCreated';
} | {
    result: 'installed';
    stderr: string;
    stdout: string;
};
/**
 * Creates, checks package changes, install necessary dependencies and call config hooks
 */
export declare class PackageSync {
    private readonly cacheFilePath;
    private options;
    constructor(options?: Partial<PackageSyncOptions>);
    static GET_NORMALIZED_OPTIONS(options: Partial<PackageSyncOptions>): PackageSyncOptions;
    updateCache(): Promise<PackageSyncResult>;
    check(): Promise<PackageSyncResult>;
    softCheck(): Promise<PackageSyncResult>;
    installNew(): Promise<PackageSyncResult>;
    private isSamePackages;
    private getPackageDifference;
    private getCurrentPackageData;
    private getPackageCacheData;
}
export default PackageSync;
