"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const path_1 = require("path");
const utils = require("./utils");
/**
 * Creates, checks package changes, install necessary dependencies and call config hooks
 */
// tslint:disable:export-name
class PackageSync {
    get cacheFilePath() {
        const { cacheDir } = this.options;
        return path_1.resolve(cacheDir, 'package.json');
    }
    constructor(options = {}) {
        const normalizedOptions = PackageSync.GET_NORMALIZED_OPTIONS(options);
        this.options = normalizedOptions;
    }
    static GET_NORMALIZED_OPTIONS(options) {
        const workingDir = options.workingDir !== undefined
            ? options.workingDir
            : process.cwd();
        const cacheDir = options.cacheDir !== undefined
            ? options.cacheDir
            : 'node_modules/.cache/package-sync';
        const packageFilePath = options.packageFilePath !== undefined
            ? options.packageFilePath
            : path_1.resolve(workingDir, 'package.json');
        const configFilePath = options.packageFilePath !== undefined
            ? options.packageFilePath
            : path_1.resolve(workingDir, 'package-sync.config.js');
        return {
            workingDir: path_1.resolve(workingDir),
            cacheDir: path_1.resolve(cacheDir),
            packageFilePath: path_1.resolve(packageFilePath),
            configFilePath: path_1.resolve(configFilePath),
        };
    }
    updateCache() {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            const { cacheFilePath } = this;
            const { cacheDir } = this.options;
            yield utils.makeDirDeep(cacheDir);
            const data = yield this.getCurrentPackageData();
            yield utils.writeFileData(cacheFilePath, data);
            return {
                result: 'cacheCreated',
            };
        });
    }
    check() {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            const [currentPackageData, packageCacheData,] = yield Promise.all([
                this.getCurrentPackageData(),
                this.getPackageCacheData(),
            ]);
            console.log(currentPackageData);
            console.log(packageCacheData);
            if (this.isSamePackages(currentPackageData, packageCacheData)) {
                return {
                    result: 'equal',
                };
            }
            return this.getPackageDifference(currentPackageData, packageCacheData);
        });
    }
    softCheck() {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            const { cacheFilePath } = this;
            const exists = yield utils.exists(cacheFilePath);
            if (!exists) {
                return this.updateCache();
            }
            return this.check();
        });
    }
    installNew() {
        return tslib_1.__awaiter(this, void 0, void 0, function* () {
            const softCheckResult = yield this.softCheck();
            if (softCheckResult.result !== 'diff') {
                return softCheckResult;
            }
            const { stdout, stderr } = yield utils.execCommand('npm i');
            return {
                result: 'installed',
                stdout,
                stderr,
            };
        });
    }
    isSamePackages(currentPackageData, packageCacheData) {
        // @TODO
        return currentPackageData === packageCacheData;
    }
    getPackageDifference(currentPackageData, packageCacheData) {
        // @TODO
        return {
            result: 'diff',
        };
    }
    getCurrentPackageData() {
        const { packageFilePath } = this.options;
        return utils.getFileData(packageFilePath);
    }
    getPackageCacheData() {
        const { cacheFilePath } = this;
        return utils.getFileData(cacheFilePath);
    }
}
exports.PackageSync = PackageSync;
//   async updateCache() {
//     const { cacheFilePath } = this.options;
//     await PackageHelpers.makeDirDeep(cacheFilePath);
//     const data = await this.getCurrentPackageData();
//     await this.writePackageCacheData(data);
//   }
//   async check() {
//     const [
//       currentPackageData,
//       packageCacheData,
//     ] = await Promise.all([
//       this.getCurrentPackageData(),
//       this.getPackageCacheData(),
//     ]);
//     if (this.isSamePackages(currentPackageData, packageCacheData)) {
//       return new PackageCheckResult('equal');
//     }
//     return this.getPackageDifference(currentPackageData, packageCacheData);
//   }
//   async softCheck() {
//     const { cacheFilePath } = this;
//     const exists = await PackageHelpers.exists(cacheFilePath);
//     if (!exists) {
//       await this.updateCache();
//       return new PackageCheckResult('updated');
//     }
//     const checkResult = await this.check();
//     return checkResult;
//   }
//   async installNew() {
//     const softCheckResult = await this.softCheck();
//     if (softCheckResult.result !== 'difference') {
//       return softCheckResult;
//     }
//     const { stdout, stderr } = await PackageHelpers.execCommand('npm i');
//     return new PackageCheckResult('installed', {
//       stdout,
//       stderr,
//     });
//   }
// };
// tslint:disable-next-line:no-default-export
exports.default = PackageSync;
